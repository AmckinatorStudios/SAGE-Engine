#include "TestFramework.h"

#include "sage/core/EngineContext.h"

// Единая точка входа: тесты регистрируются статически в test_*.cpp через TEST(),
// здесь только запускаем весь набор и возвращаем 0/1 (для CTest и CI).
int main() {
    // Подсистемы движка (кэш ресурсов, база ассетов, пул задач, настройки) —
    // на время прогона. Раньше это были функции-статики, то есть состояние
    // текло между проверками и порядок запуска влиял на результат; теперь у
    // набора свой контекст, и проверка может поднять собственный
    // (ScopedEngineContext), если ей нужен чистый лист.
    sage::ScopedEngineContext engineContext;
    return sagetest::RunAll();
}
