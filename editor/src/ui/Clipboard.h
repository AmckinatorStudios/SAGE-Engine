#pragma once
#include <string>

// ---------------------------------------------------------------------------
// БУФЕР ОБМЕНА.
//
// Крошечный слой, и он нужен ровно затем, что панели на SAGE UI НЕ ЗНАЮТ про
// ImGui (§30). Сегодня буфер обмена даёт ImGui, завтра — оболочка на SAGE UI
// или система напрямую; панель, вызывающая ImGui::SetClipboardText, пришлось бы
// править в оба этих дня.
//
// Реализацию ставит оболочка редактора один раз при запуске.
// ---------------------------------------------------------------------------
namespace Clipboard {

void SetText(const std::string& text);
std::string GetText();

} // namespace Clipboard
