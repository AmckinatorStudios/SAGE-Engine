#pragma once
#include "sage/project/Project.h"

// ---------------------------------------------------------------------------
// Проект в редакторе — ЭТО ПРОЕКТ ДВИЖКА, а не отдельный класс.
//
// Раньше здесь лежала собственная реализация с комментарием «проект это
// сущность редактора, движку о нём знать не нужно». Она переехала в движок
// целиком (engine/src/sage/project/Project.h) — там же написано, почему.
// Коротко: плеер открывает проекты тоже, класса редактора у него нет, и он
// разбирал тот же файл своим кодом. Две реализации одного понятия расходятся
// не «если», а «когда».
//
// Псевдоним, а не переименование по всему редактору: имя Project встречается в
// редакторе почти сотню раз, и замена его на sage::project::Project во всех
// этих местах — большая правка ради нулевой пользы. Здесь же видно, что за ним
// стоит.
using Project = sage::project::Project;
