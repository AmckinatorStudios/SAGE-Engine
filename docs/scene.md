# Сцены, компоненты и префабы

Из чего состоит сцена, как она пишется на диск и переживает смену формата.

## Содержание

- [Система сцен и сериализация](#система-сцен-и-сериализация)
- [С какой сцены начинается игра](#с-какой-сцены-начинается-игра)
- [Переход между сценами](#переход-между-сценами)
- [Версия формата сцены и миграции](#версия-формата-сцены-и-миграции)
- [Префабы и сохранения игры](#префабы-и-сохранения-игры)

## Система сцен и сериализация
`Scene` — это ECS-сцена на `entt::registry`. Сущности собираются из компонентов
(`NameComponent`, `Transform`, `MeshRendererComponent`, `ScriptComponent`,
`CameraComponent`, ...);
`GameObject` — дешёвый дескриптор `{registry, entity}` с аксессорами к
компонентам. `SceneManager` держит несколько сцен и переключает активную:

```cpp
SceneManager sceneManager;
Scene& level1 = sceneManager.CreateScene("Level1");
GameObject obj = level1.CreateObject("MyCube");
obj.GetTransform().Position = {0.0f, 1.0f, 0.0f};
obj.Renderer().Ref = MeshRef{MeshRef::Type::Cube, ""};
obj.Renderer().MeshPtr = ResourceManager::Instance().GetCube();

// Сохранить сцену на диск (человекочитаемый JSON, расширение .sage)
sceneManager.SaveScene("Level1", "assets/scenes/level1.sage");

// Загрузить сцену обратно (сам пересоздаст GPU-ресурсы мешей)
sceneManager.LoadScene("assets/scenes/level1.sage");
```

Обход сущностей — через ECS-view (`scene.Registry().view<...>()`) или готовую
систему `sage::ecs::ForEachRenderable(scene, ...)`. Для снапшотов в памяти
(undo/redo, Play-режим) есть строковые варианты — `SceneSerializer::SaveToString`/
`LoadFromString`, тот же JSON, без обращения к диску.

Файл `.sage` — обычный JSON, можно открыть и посмотреть/поправить руками:
```json
{
  "sage_scene_version": 1,
  "name": "Level1",
  "objects": [
    {
      "id": 1, "name": "Cube1",
      "position": {"x":0,"y":0,"z":0},
      "rotation": {"x":0,"y":0,"z":0},
      "scale":    {"x":1,"y":1,"z":1},
      "color":    {"x":0.8,"y":0.3,"z":0.3},
      "mesh": {"type":"cube","path":""}
    }
  ]
}
```

Для модели вместо куба: `"mesh": {"type":"model","path":"assets/models/dragon.obj"}`.
Сущность со скриптом добавляет поле `"script": "assets/scripts/spin.lua"` и, если
скрипт объявил публичные переменные, их значения — `"scriptVars"`.

## С какой сцены начинается игра

Это решение автора, и лежит оно в проекте:

```json
{
  "sage_project_version": 1,
  "name": "Моя игра",
  "startScene": "main"
}
```

Назначается мышью: ПКМ по сцене в панели Assets → **«Сделать стартовой
сценой»**. Назначенная помечена в списке плашкой «старт», и с неё же
открывается проект.

**Почему это понадобилось.** Выбора не было вовсе: собранная игра брала
`scenes/main.sage`, а если её нет — ПЕРВУЮ ПО АЛФАВИТУ. То есть уровень,
названный «arena», молча становился началом игры, а переименование файла меняло
то, что увидит игрок при запуске. Узнать об этом можно было только собрав игру и
запустив её.

Порядок ответов у плеера — от «автор сказал» к «движок угадал»:

1. `startScene` из `project.sageproj`;
2. `scenes/main.sage`;
3. первая по алфавиту — и об этом пишется предупреждение в лог.

Сборка отказывается собираться, если названная сцена не найдена: игра при этом
запустилась бы НЕ С ТОЙ сцены и выглядела бы сломанной, а связать одно с другим
потом почти нечем.

## Переход между сценами

Из скрипта:

```lua
scene:Load("level2")   -- перейти на уровень
scene:Reload()         -- начать этот же заново
```

Это ЗАПРОС, а не действие. Скрипт зовёт его из `Update`, то есть когда движок
идёт по сущностям этой же сцены, а сам скрипт держит на них ссылки: загрузить
новую сцену прямо там — уничтожить реестр под ногами у обхода. Запрос
запоминается, а выполняет его хозяин кадра между кадрами, когда ни один скрипт
не исполняется.

**Работает и в редакторе.** Раньше `sage.scene.Load` в Play-режиме не делал
ничего — редактор писал в лог «проверяйте переходы в собранной игре», то есть
самую частую ошибку уровня (не та сцена, не тот спавн, потерянный игрок) нельзя
было увидеть там, где её правят. Теперь Play переходит на другой уровень
по-настоящему, а **Stop возвращает человеку его документ** — ту сцену, которая
была открыта, вместе с несохранённой правкой.

## Версия формата сцены и миграции

`sage_scene_version` писался в файл с самого начала и **никогда не читался**.
Это худший из вариантов: он создаёт впечатление, что о совместимости
позаботились, а на деле первое же ломающее изменение формата тихо испортило бы
все старые сцены — без ошибки, без предупреждения, просто «объекты почему-то не
там».

Теперь файл несёт номер, а загрузчик прогоняет его через цепочку миграций до
текущей. Каждая миграция — маленькая функция «из N в N+1», которая правит
**JSON, а не сцену**: правка на уровне JSON не зависит от того, как сегодня
выглядят компоненты, и потому не устаревает вместе с ними. Иначе миграцию
пришлось бы переписывать каждый раз, когда меняется структура, ради которой она
и была написана.

Три правила, каждое проверено тестом:

* **Нет номера — значит версия 1.** Сцены, сохранённые до появления проверки,
  не за что винить, и загружаться они обязаны.
* **Файл новее движка — отказ, а не половинчатое чтение.** Половина полей
  такому движку незнакома, и молча потерять их хуже, чем честно отказаться.
* **Миграция идемпотентна.** Повторный прогон актуального файла не меняет
  ничего: иначе сохранение-загрузка по кругу дрейфовали бы, а это самый
  неприятный вид порчи данных — медленный и незаметный.

Версия растёт при ЛОМАЮЩЕМ изменении. Добавление необязательного поля её не
двигает: старые файлы читаются без него как раньше.

**Живут миграции отдельным файлом** (`sage/scene/SceneMigrations.*`), и это не
вопрос длины. Список только растёт: каждое ломающее изменение добавляет к нему
функцию, а ни одну из прежних удалить нельзя — сцена, сохранённая два года
назад, обязана открыться. Держать его посреди сериализатора значит обречь тот
файл расти вместе с ним, хотя код текущего формата не меняется. Разделение
проходит по смыслу: здесь ИСТОРИЯ формата, в `SceneSerializer.cpp` — его
СЕГОДНЯШНИЙ вид, и, читая один, про другой знать не нужно. Номер текущей версии
объявлен рядом с цепочкой (`SceneMigrations.h`): разъехаться им значит либо
прогнать лишнюю миграцию, либо не прогнать нужную. Общий словарь формата —
векторы в JSON и обратно — в `SceneJson.h`, потому что им пользуются оба файла,
а два словаря одного формата однажды разойдутся.

## Префабы и сохранения игры

### Префабы: сущность как шаблон

Префаб — сущность с потомками, сохранённая в `.sageprefab`. Формат тот же, что
у сцены: префаб — это мини-сцена с одним корнем, и потому у него бесплатно
появляются и загрузчик, и миграция версий. Отдельный формат означал бы второй
сериализатор, который надо держать в согласии с первым, и первое же новое поле
компонента разошлось бы между ними.

В редакторе: `Save as Prefab` в контекстном меню иерархии, двойной клик по
файлу в панели Assets ставит копию в сцену.

У префаба есть **3D-обложка** — такая же, как у материала, и по той же причине.
Материал показывался квадратиком цвета, префаб — синим прямоугольником с
надписью PREFAB, одинаковым у всех: чем «Ящик_02» отличается от «Ящик_03»,
можно было узнать, только поставив оба в сцену. Обложка снимается тем же
проходом, что и вьюпорт, с фиксированным студийным светом; префаб для неё
разбирается тем же `InstantiatePrefab`, что и для настоящей сцены (отдельный
«облегчённый разбор для картинки» означал бы второй загрузчик префабов, и
обложка расходилась бы с тем, что человек получит в мире). В инспекторе она
крупная и вращается мышью. Модели (`.obj`/`.gltf`/`.glb`/`.sagemesh`) получают
обложку там же и так же.

У каждой обложки СВОЙ буфер (см. `key` в `editor/src/AssetPreview.h`). Это не
мелочь: обложки снимаются по одной за кадр и запоминаются, а показываются все
сразу, и с общим буфером все запомненные хендлы указывали бы на одну текстуру —
вся папка показывала бы то, что нарисовали последним. Пока обложка была только
у материала и в папке он лежал один, это было незаметно.

**Чего не было и что чинилось.** Код префабов жил внутри `EditorLayer.cpp`, в
безымянном пространстве имён, и потому был недоступен никому, кроме редактора.
Для игры про постройку из блоков это значит, что главной её операции — поставить
заготовленный объект в мир — из скрипта не существовало: игра либо собирала
объект по компонентам вручную при каждом спавне, либо не собирала вовсе.
Реализация переехала в `sage/scene/Prefab.h`, а редактор стал ещё одним её
потребителем, наравне с Lua:

```lua
local id = sage.scene.SpawnPrefab("assets/turret.sageprefab", Vec3.new(3, 0, 4))
sage.scene.SavePrefab(id, "assets/turret_v2.sageprefab")
```

Разобранный префаб **кэшируется** по пути: игра, которая ставит тысячу блоков,
иначе тысячу раз разбирала бы один и тот же JSON — а это самая частая операция
такой игры.

Рантайм-поля копии сбрасываются: тело физики, сустав, поза анимации, снятая
карта отражений и накопитель частиц принадлежат ЭКЗЕМПЛЯРУ, а не шаблону.
Скопируй их как есть — и две сущности делили бы один дескриптор тела, а удаление
одной уносило бы физику другой.

### Сохранения: прогресс игрока, а не сцена

Сериализовалась только сцена — **редакторный формат**. Прогресс игрока сохранять
было нечем, и игре оставалось либо писать файлы в обход движка, либо не
сохраняться вовсе.

Разница принципиальна. Сцена — исходник: какие объекты в уровне, где стоят,
какие компоненты. Она одинакова у всех игроков и лежит рядом с игрой. Прогресс —
противоположное: принадлежит одному человеку, меняется каждую минуту и не имеет
отношения к расстановке объектов в редакторе. Сохранять прогресс сериализацией
сцены значит записывать в файл игрока всю геометрию уровня и при загрузке
подменять ему уровень на тот, что был при сохранении: **любая правка игры
перестаёт доходить до тех, кто уже начал играть**.

```lua
sage.save.Write("main", {
    day = 12, hp = 80, name = "Робинзон",
    inventory = {"доска", "верёвка"},
    base = { x = 3, y = 0, z = -7 },
}, 1)                                  -- последний аргумент — версия формата

-- Развёрнутая форма: метка слота, отказ от сжатия, отказ от резервной копии.
sage.save.Write("main", data, {
    version  = 3,
    meta     = { chapter = "Пещера", playtime = 7200, hero = "Робинзон" },
    compress = true,   -- по умолчанию
    backup   = true,   -- по умолчанию
})

local s = sage.save.Read("main")       -- таблица или nil
for _, slot in ipairs(sage.save.Slots()) do   -- для меню «Продолжить»
    -- Метка здесь уже есть: карточка слота рисуется БЕЗ чтения прогресса.
    print(slot.name, slot.savedAt, slot.version, slot.meta.chapter, slot.bytes)
end

if sage.save.HasBackup("main") then    -- откат на прошлое сохранение
    sage.save.RestoreBackup("main")
end
```

### Формат слота: заголовок отдельно от прогресса

Слот был одним JSON-объектом, и читался он ровно один раз — целиком. Три беды
следовали из этого сразу.

| Было | Стало |
|---|---|
| Меню «Продолжить» разбирало КАЖДОЕ сохранение целиком ради строчки «Слот 3, вчера»: восемь слотов по пять мегабайт — сорок мегабайт разбора на открытие меню, и весь результат выбрасывался | Заголовок лежит первым и читается отдельно: сотня байт на слот (`sage.save.Info`, `Slots`) |
| Текст писался с отступами, а прогресс — это тысячи коротких чисел и повторяющихся ключей | Прогресс жмётся deflate (и только если стало меньше: у крошечного слота заголовок сжатия больше выигрыша) |
| Переименование поверх атомарно, но старое всё равно стирает: игра, сохранившаяся в состояние, из которого не выбраться, уносила единственную рабочую копию | Прошлое сохранение отходит в `<слот>.sagesave.bak`; `RestoreBackup` возвращает его КОПИЕЙ, чтобы откат не был односторонним |

```
"SAGESAVE" u32:формат u32:флаги u32:длина заголовка u32:размер u64:сумма
[заголовок — JSON: версия игры, время, МЕТКА слота]
[прогресс  — JSON, сжатый deflate или как есть]
```

* **Метка слота** (`meta`) — глава, время игры, имя героя, путь к скриншоту.
  Лежит в заголовке, а не в прогрессе: меню не должно грузить прогресс, чтобы
  подписать карточку. Кривая метка не роняет сохранение — прогресс важнее
  подписи к нему.
* **Контрольная сумма проверяется ДО разбора.** «Сохранение повреждено» честнее,
  чем прогресс с половиной значений: во втором случае игрок теряет партию молча.
  Битый слот при этом всё равно виден в списке — его надо уметь удалить.
* **Прогресс разбирается ПЕРЕД записью.** Кривой JSON, записанный в файл,
  обнаружился бы при загрузке, то есть у игрока.
* **Старый формат читается по-прежнему.** У игроков уже есть такие файлы, и
  первое же обновление игры не имеет права их потерять; следующая запись
  переводит слот на новый формат сама.

* **Где лежит.** В пользовательском каталоге (`$XDG_DATA_HOME` / `~/.local/share`,
  на Windows `%APPDATA%`), а не рядом с игрой: папка установки бывает доступна
  только на чтение (Program Files, каталог Steam), и запись туда либо молча не
  проходит, либо требует прав, которых у игры нет.
* **Как пишется.** Во временный файл, `fsync`, затем переименование поверх
  старого. Переименование в пределах тома атомарно, и это единственный способ
  пережить падение посреди записи: иначе игрок теряет не текущее сохранение, а
  ВСЁ — файл остаётся обрезанным, а прошлого уже нет. `fsync` здесь не
  перестраховка: `flush()` доводит байты только до ядра, и при отключении
  питания сразу после сохранения на диск успевает лечь одно переименование —
  на месте прошлого сохранения оказывается ПУСТОЙ файл.
* **Версия формата** у каждого слота — та же дисциплина, что у сцен. Игра меняет
  структуру прогресса чаще, чем формат уровней.
* **Нет слота — `nil`, а не пустая таблица.** «Сохранения нет» и «сохранение
  пустое» — разные вещи; спутав их, новый игрок попадает в конец игры с нулями.
* **Имя слота обеззараживается.** Оно приходит из поля ввода или имени
  персонажа, и `../../.bashrc` означал бы запись за пределы папки сохранений.
* **Циклы в таблицах не роняют игру.** `t.self = t` ловится по факту повторной
  встречи, а не глубиной рекурсии: каждый уровень держит открытый обход таблицы,
  и три десятка вложенных обходов переполняют стек Lua — причём падает игра не
  на сохранении, а позже, при закрытии состояния.

**Выход из игры — тоже момент сохранения, и о нём игре надо сказать.** Хук
`OnQuit()` зовётся на ЛЮБОМ пути закрытия: кнопка меню, `sage.game.Quit()`,
крестик окна, Stop в Play-режиме редактора. Без него игра не узнавала о
закрытии никогда и теряла всё, что случилось после последнего автосохранения, —
молча, что с точки зрения человека звучит как «игра не сохранила последние
двадцать минут». Хук срабатывает один раз, до разрушения сцены (внутри ещё
можно ходить по сущностям и звать `sage.save.Write`), а ошибка в нём не мешает
выходу: игра уже закрывается, и падать на прощание — худшее, что можно сделать.

```lua
function OnQuit()          -- у объектного скрипта — OnQuit(entity)
    SaveProgress()
end
```

На полноразмерном проекте это работает по-настоящему: построенная игроком
постройка (168 блоков в проверочном прогоне), инвентарь, поза игрока, шкалы
выживания, время суток и пройденный путь пишутся автосохранением раз в
полминуты, при выходе и по кнопке «Сохранить» в меню, а восстанавливаются при
следующем запуске. Автосохранение по
таймеру, а не по событию «поставил блок»: блоки ставят пачками, и запись на
каждый означала бы сотни записей в минуту.
