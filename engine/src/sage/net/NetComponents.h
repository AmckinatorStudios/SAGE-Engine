#pragma once

// ---------------------------------------------------------------------------
// Компонент сетевой репликации — рядом со своей системой (см. sage/net), как и
// остальные компоненты движка: scene/Components.h остался ЗОНТИКОМ, а не
// местом, где компоненты живут.
// ---------------------------------------------------------------------------

// Участие сущности в репликации. СЕРВЕР включает такие сущности в снапшот
// (позиция/поворот/масштаб, цвет и вид примитива), клиент применяет снапшот к
// своей сцене по id и сглаживает движение интерполяцией — см.
// sage::net::NetworkSystem.
//
// Маркер без данных: что именно реплицировать, сеть решает по составу
// сущности, а не по настройкам внутри компонента. Поле нужно только потому,
// что пустой тип entt хранит отдельным путём, а сериализация и инспектор
// работают с обычным.
struct NetReplicatedComponent {
    bool Placeholder = true;
};
