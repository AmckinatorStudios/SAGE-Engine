#pragma once
#include "sage/ui/core/UIDocument.h"
#include "sage/ui/style/UIStyle.h"

// ---------------------------------------------------------------------------
// ПОКАЗАТЕЛЬНЫЙ ИНТЕРФЕЙС (§126 ТЗ).
//
// Это НЕ игровой экран и не заготовка меню. Это техническая витрина
// возможностей ядра: каждая её часть существует затем, чтобы показать одну
// конкретную возможность системы и сломаться, если та перестанет работать.
//
//   фон           — градиент, декоративные формы, слои
//   верхняя панель— горизонтальная раскладка, подгонка по содержимому
//   карточки      — сетка, девятина, вложенные группы, тень и свечение
//   боковая панель— вертикальная раскладка, маска со скруглением, прокрутка
//   типографика   — выравнивание, перенос, многоточие, обводка, тень
//   элементы      — кнопка, галка, ползунок, полоса, поле ввода
//
// Собран КОДОМ, а не файлом, намеренно: витрина обязана существовать в любой
// сборке и не зависеть от того, лежит ли рядом ассет.
// ---------------------------------------------------------------------------
namespace sage::ui {

// Собрать витрину в документе (документ очищается).
void UIBuildShowcase(UIDocument& doc, UITheme& theme);

} // namespace sage::ui
